import { useEffect } from 'react';
import '../styles/globals.css'
import '../styles/removeableListItem.css';

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />
}

export default MyApp
